## run environment 

```sh
docker-compose up -d
```

## enter on docker image bash

```sh
docker exec -it code_pix_go bash
```

## run application and start a gRPC server

```sh
go run main.go grpc
```